replicaCount: 2

image:
  repository: harbor.k8s.laczyk.com/lab-app/flask
  tag: v1
  pullPolicy: IfNotPresent

service:
  type: ClusterIP
  port: 80
  targetPort: 5000

ingress:
  enabled: true
  className: nginx
  host: lab.k8s.laczyk.com
